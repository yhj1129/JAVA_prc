package ch9;

public class ComputerTest {
    public static void main(String[] args) {
        Desktop desktop = new Desktop();
        // Computer desktop = new Desktop(); 변수로 사용할 수 있는 것은 Computer에 정의된 display, typing, turnon, turnoff만 가능하다
        // Desktop에 추가적으로 구현한 메서드는 사용할 수 없다!

    }
}
